/* eslint-disable no-undef */

import 'react-native';
import React from 'react';
import OpenSans from './SFPro';

import renderer from 'react-test-renderer';

it('renders correctly', () => {
  renderer.create(<OpenSans />);
});
